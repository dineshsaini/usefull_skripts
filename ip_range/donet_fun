ip="$1"; #recieved ip for process

#echo "ip recieved ==> $ip";

function proc_ping() {
	ip="$1";
	ping -c 1 $ip 1>& /dev/null;
       	if [[ "$?" -eq 0 ]]; then
		echo "ip [$ip] is live.";
	fi;
}


proc_ping $ip;
